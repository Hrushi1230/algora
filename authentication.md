# Algora — Section 2: Authentication (5 pages)

Image-generation prompts for GPT-4o / "gpt image 2". Each prompt renders ONE complete, pixel-perfect SaaS screen as a single screenshot. Copy-paste one block at a time.

**Brand recap (do not deviate):**
Algora — a gamified platform where CS students master algorithms through synchronized visualization, code, and plain-English explanation. Tagline: *"See the algorithm think."*
Light theme only. Paper background, ink text, one teal accent. Instrument Sans (headings/body) + JetBrains Mono (code, labels, stats).

**Shared visual system (paste inside every prompt):**
- Background warm off-white paper #F7F9F8; elevated cards pure white #FFFFFF; hairline borders #E4E9E7.
- Text near-black ink #0E1513 for headings, muted slate #5B6763 for secondary text.
- ONE accent: teal #0E9C86 (highlight #14B8A6) — used only for primary buttons, active states, focus rings, progress, links, and the logo glyph.
- NO dark backgrounds anywhere, NO black panels, NO purple, NO rainbow gradients, NO glowing blobs/orbs, NO stock photos of people, NO emojis. Flat solid colors, crisp edges, soft realistic light shadows only.
- Type: Instrument Sans for headlines (large, tight, geometric); JetBrains Mono for code, badges, stats, field labels. Corners 12–16px, 1px hairline borders, 8px input focus ring in pale teal, generous whitespace, high contrast, WCAG-AA. Ultra high-fidelity, 4k, clean.
- Auth layout convention: a centered 16:9 landscape screen (single viewport, NOT a long scrolling page). Split layout — a left FORM panel on white/paper and a right BRAND panel showing a calm product motif. Thin sticky top bar with the wordmark. Small mono footer line.

---

## PROMPT — 9. Sign up (light)

Generate ONE complete, pixel-perfect SaaS authentication screen as a single landscape screenshot (aspect ratio 16:9), rendered as a real product UI — NOT an illustration, NOT a collage. Render a single full viewport, nothing cropped.

PRODUCT: "algora" — a gamified algorithm-learning platform. This is the SIGN UP screen. Tagline: "See the algorithm think."

VISUAL SYSTEM — LIGHT THEME (strict): Background warm off-white paper #F7F9F8; cards pure white #FFFFFF; hairline borders #E4E9E7. Text near-black ink #0E1513 headings, muted slate #5B6763 secondary. ONE accent teal #0E9C86 (highlight #14B8A6) for primary button, active/focus states, links, logo glyph only. NO dark backgrounds, NO black panels, NO purple, NO gradients/blobs, NO stock photos of people, NO emojis. Flat colors, crisp edges, soft light shadows. Type: Instrument Sans headlines; JetBrains Mono for labels, badges, stats. Corners 12–16px, 1px borders, pale-teal focus ring, WCAG-AA, 4k ultra high-fidelity.

TOP BAR (thin, white, hairline bottom border): left "algora" wordmark in mono with a small teal two-node graph glyph. Right, muted slate mono line "Already have an account?" + teal text link "Log in".

LAYOUT — SPLIT (two columns):
LEFT FORM PANEL (on paper, form on a white card with hairline border, centered, max width ~440px):
- Small mono pill badge "◆ CREATE ACCOUNT" (teal on pale-teal tint).
- Ink headline "Start learning free." (period as a small teal square). Slate subhead: "480 lessons, 60+ visualized algorithms, no credit card."
- A full-width ghost outline button "Continue with Google" with a small Google mark, and a second ghost "Continue with GitHub" with a GitHub mark.
- A thin "or sign up with email" divider (hairline rule with centered mono label).
- Form fields, each with a mono label above a white input with hairline border: "Full name", "Email", "Password" (with a small show/hide eye icon and a teal strength meter reading "Strong"). A small slate helper under password: "8+ characters, one number."
- A checkbox row: "I agree to the Terms and Privacy Policy" with "Terms" and "Privacy Policy" as teal links.
- Solid teal full-width primary button "Create account".
- Small mono slate reassurance line below: "No credit card. Cancel anytime."

RIGHT BRAND PANEL (soft pale-teal tinted panel, never dark): a calm product motif — a small WHITE visualizer card floating with hairline border showing a binary tree of 6 nodes (one teal "Current", two pale-teal "Visited"), a mini XP progress ring reading "Lvl 1", and a mono caption "See the algorithm think." Below, three mono trust stats in a row: "120k+ students · 480 lessons · 4.9★". Keep it airy, flat, no photos.

FOOTER: small centered mono slate line "© 2026 Algora · Secure sign up · Reduced-motion friendly".

IMPORTANT: ONE clean single-screen light UI, realistic legible placeholder copy (no lorem, no misspellings), consistent alignment, single cohesive light design language matching the Algora marketing pages.

---

## PROMPT — 10. Log in (light)

Generate ONE complete, pixel-perfect SaaS authentication screen as a single landscape screenshot (aspect ratio 16:9), rendered as a real product UI — NOT an illustration, NOT a collage. Single full viewport, nothing cropped.

PRODUCT: "algora" — a gamified algorithm-learning platform. This is the LOG IN screen. Tagline: "See the algorithm think."

VISUAL SYSTEM — LIGHT THEME (strict, identical to Sign up): Background paper #F7F9F8; cards white #FFFFFF; hairline borders #E4E9E7. Ink #0E1513 headings, slate #5B6763 secondary. ONE accent teal #0E9C86 (highlight #14B8A6) for primary button, focus/active states, links, logo glyph only. NO dark backgrounds, NO black panels, NO purple, NO gradients/blobs, NO stock photos of people, NO emojis. Flat colors, crisp edges, soft light shadows. Instrument Sans headlines; JetBrains Mono labels/badges/stats. Corners 12–16px, 1px borders, pale-teal focus ring, WCAG-AA, 4k.

TOP BAR (thin, white, hairline bottom border): left "algora" wordmark + teal two-node graph glyph. Right muted slate mono "New to Algora?" + teal link "Create account".

LAYOUT — SPLIT (two columns):
LEFT FORM PANEL (on paper, form on a centered white card, max width ~420px):
- Small mono pill badge "◆ WELCOME BACK" (teal on pale-teal).
- Ink headline "Log in to keep your streak." (period as a small teal square). Slate subhead: "Pick up right where you left off."
- Ghost outline "Continue with Google" and ghost "Continue with GitHub", each with its brand mark.
- Thin "or log in with email" divider.
- Fields with mono labels: "Email"; "Password" with a small show/hide eye icon. On the password label row, a right-aligned teal text link "Forgot password?".
- A checkbox row "Keep me signed in".
- Solid teal full-width primary button "Log in".
- Small mono slate line: "Protected by encrypted sessions."

RIGHT BRAND PANEL (soft pale-teal tinted panel, never dark): a calm motif — a small WHITE streak card floating with hairline border showing a teal flame, bold "23-day streak", and a M–S week row with teal checkmarks; below it a mini teal XP progress ring "Lvl 12", "2,150 / 2,400 XP". Mono caption "Consistency compounds." Airy, flat, no photos.

FOOTER: small centered mono slate line "© 2026 Algora · Secure log in · Reduced-motion friendly".

IMPORTANT: ONE clean single-screen light UI, realistic legible placeholder copy (no lorem, no misspellings), consistent alignment, matching the Algora design language.

---

## PROMPT — 11. Forgot password (light)

Generate ONE complete, pixel-perfect SaaS authentication screen as a single portrait screenshot (aspect ratio 9:16), rendered as a real product UI — NOT an illustration, NOT a collage. Single full viewport, nothing cropped.

PRODUCT: "algora" — a gamified algorithm-learning platform. This is the FORGOT PASSWORD (request reset link) screen. Tagline: "See the algorithm think."

VISUAL SYSTEM — LIGHT THEME (strict, identical to the other auth screens): paper #F7F9F8; white cards #FFFFFF; hairline borders #E4E9E7. Ink #0E1513 headings, slate #5B6763 secondary. ONE accent teal #0E9C86 (highlight #14B8A6) for primary button, focus, links, glyph only. NO dark backgrounds, NO black panels, NO purple, NO gradients/blobs, NO stock photos of people, NO emojis. Flat colors, crisp edges, soft light shadows. Instrument Sans headlines; JetBrains Mono labels. Corners 12–16px, 1px borders, pale-teal focus ring, WCAG-AA, 4k.

TOP BAR (thin, white, hairline bottom border): left "algora" wordmark + teal two-node graph glyph. Right muted slate mono "Remembered it?" + teal link "Back to log in".

LAYOUT — SINGLE CENTERED CARD (no split; centered white card on paper, max width ~440px, generous whitespace around it):
- A small circular pale-teal icon tile at the top with a teal outline key/lock line icon (24px, consistent stroke).
- Small mono pill badge "◆ RESET PASSWORD" (teal on pale-teal).
- Ink headline "Forgot your password?" (period as small teal square). Slate subhead: "Enter your account email and we'll send a secure reset link."
- One field: mono label "Email" over a white input with hairline border (shown focused with a pale-teal ring).
- Solid teal full-width primary button "Send reset link".
- A ghost outline full-width secondary button "Back to log in".
- Small mono slate helper line: "The link expires in 30 minutes."

BELOW THE CARD: a subtle slate mono note "Didn't get it? Check spam or" + teal link "resend".

FOOTER: small centered mono slate line "© 2026 Algora · Account security".

IMPORTANT: ONE clean single-screen light UI, realistic legible placeholder copy (no lorem, no misspellings), calm and reassuring, matching the Algora design language.

---

## PROMPT — 12. Reset password (light)

Generate ONE complete, pixel-perfect SaaS authentication screen as a single landscape screenshot (aspect ratio 16:9), rendered as a real product UI — NOT an illustration, NOT a collage. Single full viewport, nothing cropped.

PRODUCT: "algora" — a gamified algorithm-learning platform. This is the RESET PASSWORD (set a new password, arrived from email link) screen. Tagline: "See the algorithm think."

VISUAL SYSTEM — LIGHT THEME (strict, identical to the other auth screens): paper #F7F9F8; white cards #FFFFFF; hairline borders #E4E9E7. Ink #0E1513 headings, slate #5B6763 secondary. ONE accent teal #0E9C86 (highlight #14B8A6) for primary button, focus, links, glyph only. NO dark backgrounds, NO black panels, NO purple, NO gradients/blobs, NO stock photos of people, NO emojis. Flat colors, crisp edges, soft light shadows. Instrument Sans headlines; JetBrains Mono labels. Corners 12–16px, 1px borders, pale-teal focus ring, WCAG-AA, 4k.

TOP BAR (thin, white, hairline bottom border): left "algora" wordmark + teal two-node graph glyph. Right muted slate mono line showing the account context "arjun@stanford.edu".

LAYOUT — SINGLE CENTERED CARD (no split; centered white card on paper, max width ~440px):
- A small circular pale-teal icon tile with a teal outline shield/lock line icon (24px).
- Small mono pill badge "◆ CHOOSE A NEW PASSWORD" (teal on pale-teal).
- Ink headline "Set a new password." (period as small teal square). Slate subhead: "Make it strong — you won't need it often."
- Field 1: mono label "New password" over a white input with a show/hide eye icon and a segmented teal strength meter reading "Strong".
- A small requirements checklist in slate mono with teal checkmarks: "✓ 8+ characters", "✓ One number", "✓ One uppercase letter".
- Field 2: mono label "Confirm password" over a white input; a small teal inline confirmation chip "Passwords match".
- Solid teal full-width primary button "Update password".
- Small mono slate line: "You'll be signed in automatically after resetting."

FOOTER: small centered mono slate line "© 2026 Algora · Account security".

IMPORTANT: ONE clean single-screen light UI, realistic legible placeholder copy (no lorem, no misspellings), consistent alignment, matching the Algora design language.

---

## PROMPT — 13. Email verification (light)

Generate ONE complete, pixel-perfect SaaS authentication screen as a single landscape screenshot (aspect ratio 16:9), rendered as a real product UI — NOT an illustration, NOT a collage. Single full viewport, nothing cropped.

PRODUCT: "algora" — a gamified algorithm-learning platform. This is the EMAIL VERIFICATION screen (confirm your email after sign up, with a 6-digit code option). Tagline: "See the algorithm think."

VISUAL SYSTEM — LIGHT THEME (strict, identical to the other auth screens): paper #F7F9F8; white cards #FFFFFF; hairline borders #E4E9E7. Ink #0E1513 headings, slate #5B6763 secondary. ONE accent teal #0E9C86 (highlight #14B8A6) for primary button, active states, focus, links, glyph only. NO dark backgrounds, NO black panels, NO purple, NO gradients/blobs, NO stock photos of people, NO emojis. Flat colors, crisp edges, soft light shadows. Instrument Sans headlines; JetBrains Mono for the code inputs and labels. Corners 12–16px, 1px borders, pale-teal focus ring, WCAG-AA, 4k.

TOP BAR (thin, white, hairline bottom border): left "algora" wordmark + teal two-node graph glyph. Right muted slate mono "Wrong email?" + teal link "Change it".

LAYOUT — SINGLE CENTERED CARD (no split; centered white card on paper, max width ~460px):
- A small circular pale-teal icon tile with a teal outline envelope/check line icon (24px).
- Small mono pill badge "◆ VERIFY EMAIL" (teal on pale-teal).
- Ink headline "Check your inbox." (period as small teal square). Slate subhead: "We sent a 6-digit code to arjun@stanford.edu. Enter it below to confirm your account."
- A row of SIX separate square code-input boxes in JetBrains Mono, each with a hairline border; the first three filled ("4 8 1") with the third box shown active/focused with a teal ring, the rest empty.
- Solid teal full-width primary button "Verify & continue".
- A slate mono line with a countdown: "Resend code in 00:42" where the timer is in mono; once elapsed it becomes a teal link "Resend code".
- A thin hairline divider, then a slate mono helper: "Didn't get the email? Check spam, or" + teal link "use a different address".

BELOW THE CARD: a subtle progress row of three small mono step dots — "Account · Verify · Start" — with "Verify" active in teal, showing this is step 2 of onboarding.

FOOTER: small centered mono slate line "© 2026 Algora · Secure verification · Reduced-motion friendly".

IMPORTANT: ONE clean single-screen light UI, realistic legible placeholder copy (no lorem, no misspellings), consistent alignment, single cohesive light design language matching the Algora marketing and auth screens.
